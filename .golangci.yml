run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
    - misspell
    - unconvert
    - unused
    - gofmt
    - revive

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
